# Environment variables for PDF Password Server

# Server Configuration
PORT=3000
NODE_ENV=production

# Security - Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this
PDF_PASSWORD=your-pdf-password-here

# CORS Configuration
ALLOWED_ORIGINS=https://yourdomain.com
